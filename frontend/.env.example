# ============================================================
# 预测大师 前端构建配置模板
# 使用：复制本文件为 frontend/.env 并按需修改。
# 本文件提交入库；frontend/.env 不入库（含项目 ID 等敏感值）。
#
# 所有变量均为「可选」：缺省时使用 src/config.ts 内置默认值（测试网 4801）。
# 纯静态站点，环境变量在构建时注入，改完需重新 npm run build 并重新部署。
# ============================================================

# ---- 目标链（正式上线切换的关键开关）----
# 4801 = World Chain Sepolia 测试网（联调/测试，默认）
# 480  = World Chain 主网（正式上线）
VITE_CHAIN_ID=4801

# 链显示名（缺省按 CHAIN_ID 映射，一般无需设置）
# VITE_CHAIN_NAME=World Chain Sepolia

# RPC URL（缺省按链使用 Alchemy 公网节点）
# 部署到阿里云（纯 IP、手机网络可直连）时建议走服务器自带的 JSON-RPC 反代：
#   VITE_RPC_URL=http://8.141.100.69:8085/rpc
# 该地址由部署目录中 serve.mjs 的 /rpc 路由转发到下方 Alchemy 节点，可绕开手机 DNS 对 alchemy.com 的解析/拦截。
# 测试网: https://worldchain-sepolia.g.alchemy.com/public
# 主网:   https://worldchain-mainnet.g.alchemy.com/public
# VITE_RPC_URL=http://8.141.100.69:8085/rpc

# 区块浏览器（缺省按链映射）
# VITE_EXPLORER_URL=https://worldchain-sepolia.explorer.alchemy.com

# ---- WalletConnect Cloud Project ID ----
# 需要自行注册（免费）：https://cloud.walletconnect.com → Projects → 复制 Project ID
# 当前源码内置一个开发用默认值；正式上线建议换成自有 Project ID。
# VITE_PROJECT_ID=

# ============================================================
# 合约地址覆盖（每次上线都要核对！）
# 不设置时使用 src/config.ts 内置双链地址表；设置了则覆盖当前链对应地址。
#
# 获取途径：forge script 部署后在终端输出的地址 / worldscan 合约页。
# 正式上线清单（需替换为 480 主网的部署输出地址）：
#   CornToken        → 交易代币
#   PredictionMarket → 预测市场（代理地址）
#   OracleAdapter    → 价格裁决
#   GovCornToken     → 治理质押代币（GovernanceToken）
#   TokenHouse       → 代议制治理 Governor（TokenHouse）
#   HumanHouse       → 争议处理 HumanHouse
# ============================================================
# VITE_CORN_TOKEN_ADDRESS=0x...
# VITE_PREDICTION_MARKET_ADDRESS=0x...
# VITE_ORACLE_ADAPTER_ADDRESS=0x...
# VITE_GOV_CORN_TOKEN_ADDRESS=0x...
# VITE_TOKEN_HOUSE_ADDRESS=0x...
# VITE_HUMAN_HOUSE_ADDRESS=0x...